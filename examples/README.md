# Example report

`example-report.html` is a real run, not a mockup: 17 documents from the bundled
Nutrient sample corpus against Anthropic, OpenAI and Bedrock on 2026-08-17 —
102 calls, all successful.

Open it before configuring anything. It is what `costlab` produces on your own
documents, with your own keys and your own models, and it is measured on our
sample documents rather than yours — the provenance block at the top says so.
